<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bustrack">

        <!-- Main Launcher -->
        <activity
            android:name=".ui.driver.DriverDashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activities -->
        <activity android:name=".ui.driver.AttendanceActivity" android:exported="false" />
        <activity android:name=".ui.driver.TransitManifestActivity" android:exported="false" />
        <activity android:name=".ui.driver.navigationStart" android:exported="false" />
        <activity android:name=".ui.driver.navigationEnd" android:exported="false" />
        <activity android:name=".ui.driver.NotificationActivity" android:exported="false" />
        <activity android:name=".ui.driver.DriverProfileActivity" android:exported="false" />
        <activity android:name=".ui.driver.AccountDetailsActivity" android:exported="false" />
        <activity android:name=".ui.driver.NotificationSettingsActivity" android:exported="false" />
        <activity android:name=".ui.driver.NavigationPreferencesActivity" android:exported="false" />

    </application>

</manifest>
